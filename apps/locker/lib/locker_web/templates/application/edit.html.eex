<h1>Edit Application</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.application_path(@conn, :update, @application)) %>

<span><%= link "Back", to: Routes.application_path(@conn, :index) %></span>
