<h1>New Global session</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.global_session_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.global_session_path(@conn, :index) %></span>
