<h1>New Local session</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.local_session_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.local_session_path(@conn, :index) %></span>
